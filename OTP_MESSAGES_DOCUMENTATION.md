# OTP Messages Documentation

## نظرة عامة

تم تحديث نظام رسائل OTP ليتوافق مع معايير هيئة الاتصالات والفضاء والتقنية (CITC) في المملكة العربية السعودية.

## النماذج المعتمدة

### 🔹 نموذج 1 – لتسجيل الدخول
```
رمز التحقق الخاص بك هو: {code}
لاستخدامه في تسجيل الدخول إلى حسابك في {app_name}
صالح لمدة 5 دقائق.
```

### 🔹 نموذج 2 – لتأكيد عملية
```
رمز التحقق الخاص بك هو: {code}
لتأكيد {operation_name} في {app_name}
يرجى عدم مشاركة الرمز مع أي شخص.
صالح لمدة 5 دقائق.
```

### 🔹 نموذج 3 – للتسجيل أو إنشاء حساب جديد
```
رمز التحقق: {code}
لاستخدامه في إنشاء حسابك على {app_name}
تنبيه: لا تشارك هذا الرمز مع أي جهة.
```

## الاستخدام

### 1. إرسال OTP لتسجيل الدخول
```php
// في Controller
$result = $this->otpService->generate($phone, 'login');

// أو عبر API
POST /api/send-otp
{
    "phone": "+966501234567",
    "type": "login"
}
```

### 2. إرسال OTP لتأكيد عملية
```php
// في Controller
$result = $this->otpService->generate($phone, 'confirm', 'تغيير كلمة المرور');

// أو عبر API
POST /api/send-otp
{
    "phone": "+966501234567",
    "type": "confirm",
    "operation_name": "تغيير كلمة المرور"
}
```

### 3. إرسال OTP للتسجيل
```php
// في Controller
$result = $this->otpService->generate($phone, 'register');

// أو عبر API
POST /api/send-otp
{
    "phone": "+966501234567",
    "type": "register"
}
```

## التكوين

### ملف `config/otp_messages.php`
```php
return [
    'app_name' => env('APP_NAME', 'Slide Tech'),
    
    'templates' => [
        'login' => [
            'message' => "رمز التحقق الخاص بك هو: {code}\nلاستخدامه في تسجيل الدخول إلى حسابك في {app_name}\nصالح لمدة 5 دقائق.",
        ],
        // ... باقي النماذج
    ],
    
    'default_type' => 'login',
    'expiry_minutes' => 5,
    'max_attempts' => 3,
];
```

## متغيرات القوالب

- `{code}` - رمز التحقق
- `{app_name}` - اسم التطبيق
- `{operation_name}` - اسم العملية (لنموذج التأكيد)

## أمثلة الرسائل النهائية

### تسجيل الدخول
```
رمز التحقق الخاص بك هو: 123456
لاستخدامه في تسجيل الدخول إلى حسابك في Slide Tech
صالح لمدة 5 دقائق.
```

### تأكيد العملية
```
رمز التحقق الخاص بك هو: 789012
لتأكيد تغيير كلمة المرور في Slide Tech
يرجى عدم مشاركة الرمز مع أي شخص.
صالح لمدة 5 دقائق.
```

### التسجيل
```
رمز التحقق: 345678
لاستخدامه في إنشاء حسابك على Slide Tech
تنبيه: لا تشارك هذا الرمز مع أي جهة.
```

## التوافق مع CITC

✅ **اسم الجهة**: يتم تضمين اسم التطبيق في كل رسالة  
✅ **سبب الإرسال**: يتم توضيح الغرض من إرسال الرمز  
✅ **تحذيرات الأمان**: رسائل تحذيرية ضد مشاركة الرمز  
✅ **مدة الصلاحية**: توضيح مدة صلاحية الرمز  

## الاختبار

```php
// اختبار النماذج
$smsService = new SmsService();

// نموذج تسجيل الدخول
$result = $smsService->sendOtp('+966501234567', '123456', null, 'login');

// نموذج تأكيد العملية
$result = $smsService->sendOtp('+966501234567', '789012', null, 'confirm', 'تغيير كلمة المرور');

// نموذج التسجيل
$result = $smsService->sendOtp('+966501234567', '345678', null, 'register');
```

